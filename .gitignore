node_modules/
dist/
# Dotenv files are opaque secrets. Keep every common root and service-name
# variant out of Git; the intentional public root template stays trackable.
.env
.env.*
*.env
*.env.*
!/.env.example
*.log
.claude/
dist-server/

# Local-only, candid user-testing notes — never publish to this public repo.
docs/USER-TESTING-FEEDBACK.md
# Session handoffs live in memory and PLAN.md, never in the tree (CLAUDE.md).
HANDOFF.md
.gemini/
*.tgz
